/** Fields for the admin clinic information form (all optional for partial updates). */
export interface ClinicFormData {
  name?: string;
  tagline?: string;
  address?: string;
  phone?: string;
  email?: string;
  hours?: string;
}
